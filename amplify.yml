version: 0.2
phases:
  install:
    nodejs: 14
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm install
      - npm build
artifacts:
  baseDirectory: /dist/
  files:
    - src/**/*
    - package.json
cache:
  paths:
    - node_modules/**/*